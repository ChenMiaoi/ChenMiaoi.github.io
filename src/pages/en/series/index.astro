---
import SeriesIndexPage from "@components/pages/SeriesIndexPage.astro";
import { getSeriesMap } from "@utils/content-utils";

const seriesMap = await getSeriesMap("en");
---

<SeriesIndexPage seriesMap={seriesMap} lang="en" />
